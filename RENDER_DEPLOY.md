# Деплой на Render.com

## Быстрый старт

### Вариант 1: Через Render Dashboard (рекомендуется)

1. **Создайте аккаунт на Render.com**
   - Перейдите на https://render.com
   - Зарегистрируйтесь (можно через GitHub)

2. **Создайте новый Background Worker**
   - Нажмите "New" → "Background Worker"
   - Подключите ваш GitHub репозиторий
   - Выберите ветку `main`

3. **Настройки сервиса:**
   - **Name:** `pocsoc-trading-bot`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python PocSocSig_Enhanced.py`
   - **Plan:** `Free` (или выберите нужный план)

4. **Добавьте переменные окружения:**
   - Нажмите "Environment" в настройках сервиса
   - Добавьте следующие переменные:
     ```
     BOT_TOKEN=your_telegram_bot_token
     TWELVE_DATA_API_KEY=your_twelve_data_key (опционально)
     ALPHA_VANTAGE_KEY=your_alpha_vantage_key (опционально)
     OPENAI_API_KEY=your_openai_key (опционально)
     HTTP_PROXY=your_proxy (опционально)
     HTTPS_PROXY=your_proxy (опционально)
     ```

5. **Запустите деплой**
   - Нажмите "Create Background Worker"
   - Render автоматически начнет деплой

### Вариант 2: Через render.yaml (автоматический)

Если вы используете файл `render.yaml`, Render автоматически создаст сервис:

1. **Подключите репозиторий в Render**
2. Render автоматически обнаружит `render.yaml`
3. Создайте сервис на основе конфигурации
4. Добавьте переменные окружения вручную

## Переменные окружения

### Обязательные:
- `BOT_TOKEN` - токен Telegram бота от @BotFather

### Опциональные (нужен хотя бы один для данных):
- `TWELVE_DATA_API_KEY` - ключ от Twelve Data API
- `ALPHA_VANTAGE_KEY` - ключ от Alpha Vantage API
- `OPENAI_API_KEY` - ключ от OpenAI для GPT анализа

### Опциональные (для прокси):
- `HTTP_PROXY` - HTTP прокси
- `HTTPS_PROXY` - HTTPS прокси

## Проверка работы

1. **Проверьте логи:**
   - В Render Dashboard откройте ваш сервис
   - Перейдите на вкладку "Logs"
   - Должны увидеть: `🚀 Enhanced EUR/USD Signal Bot Starting`

2. **Проверьте в Telegram:**
   - Откройте ваш бот
   - Отправьте `/start`
   - Бот должен ответить и начать автоматический анализ

## Мониторинг

- **Logs:** Все логи бота доступны в Render Dashboard
- **Metrics:** Render показывает использование CPU и памяти
- **Uptime:** Render автоматически перезапускает сервис при сбоях

## Обновление

При push в GitHub, Render автоматически:
1. Обнаружит изменения
2. Запустит новый build
3. Развернет новую версию
4. Перезапустит сервис

## Troubleshooting

### Бот не запускается:
- Проверьте логи в Render Dashboard
- Убедитесь, что все переменные окружения установлены
- Проверьте формат `.env.example` для правильного формата

### Ошибки API:
- Проверьте, что API ключи правильные
- Убедитесь, что ключи не истекли
- Проверьте лимиты API (Twelve Data: 800/day, Alpha Vantage: 25/day)

### Таймауты:
- Free план имеет ограничения
- Рассмотрите upgrade до платного плана для стабильности

## Планы Render

- **Free:** 750 часов/месяц, возможны таймауты после бездействия
- **Starter ($7/мес):** 24/7 работа, без таймаутов
- **Standard ($25/мес):** Больше ресурсов, лучшая производительность

## Стоимость

Для непрерывной работы бота рекомендуется:
- **Free план:** Достаточно для тестирования (750 часов/мес)
- **Starter план:** Для стабильной работы 24/7 ($7/месяц)

## Безопасность

⚠️ **Важно:** Никогда не коммитьте `.env` файл в Git!
- Все секретные ключи должны быть в Environment Variables в Render
- Используйте `.env.example` как шаблон

