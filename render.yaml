services:
  - type: worker
    name: pocsoc-trading-bot
    env: python
    buildCommand: pip install --only-binary :all: -r requirements.txt || pip install -r requirements.txt
    startCommand: python PocSocSig_Enhanced.py
    plan: free

