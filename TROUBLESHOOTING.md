# 🔧 Troubleshooting Guide

## Telegram Conflict Error

### ✅ Проверка 1: Локальный бот не запущен

Вы уже проверили - локальный бот **НЕ запущен** ✓

```bash
ps aux | grep PocSocSig_Enhanced.py
# Должен показать только сам grep, не процесс бота
```

---

### ✅ Проверка 2: На Render только один сервис

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Найдите сервис `pocsoc-trading-bot`
3. Убедитесь, что:
   - ✅ Только **один** сервис активен
   - ✅ Статус: **Live** (не Suspended)
   - ✅ Нет дубликатов сервисов

**Если есть несколько сервисов:**
- Остановите все лишние (Suspend)
- Оставьте только один активный

---

### ✅ Проверка 3: Перезапуск сервиса на Render

1. В Render Dashboard найдите ваш сервис
2. Нажмите **"Manual Deploy"** → **"Clear build cache & deploy"**
3. Или просто **"Restart"** сервис
4. Подождите 2-3 минуты после остановки перед запуском

---

### ✅ Проверка 4: Логи на Render

1. Откройте Render Dashboard
2. Выберите ваш сервис
3. Перейдите на вкладку **"Logs"**
4. Проверьте, что видите:
   ```
   ✓ Database initialized
   ✓ Loaded X signals from database
   Checking for existing webhook...
   ✓ No webhook found, polling can start
   Starting polling...
   ```

**Если видите ошибки:**
- Скопируйте логи и проверьте
- Убедитесь, что все переменные окружения установлены

---

## Что делать если конфликт все еще есть

### Вариант 1: Полная остановка и запуск

1. **Остановите** сервис на Render (Suspend)
2. Подождите **5 минут**
3. **Запустите** сервис снова (Resume)

### Вариант 2: Удаление webhook вручную

Если бот все еще не запускается, удалите webhook вручную:

```bash
# Замените YOUR_BOT_TOKEN на ваш токен
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook?drop_pending_updates=true"
```

Ответ должен быть:
```json
{"ok":true,"result":true,"description":"Webhook was deleted"}
```

После этого перезапустите сервис на Render.

---

## Профилактика

### Чтобы избежать конфликтов в будущем:

1. ✅ **Всегда останавливайте локальный бот** перед деплоем на Render
2. ✅ **Используйте только один сервис** на Render
3. ✅ **Подождите 2-3 минуты** после остановки перед запуском
4. ✅ **Не используйте webhook и polling одновременно**

---

## Быстрая диагностика

### Проверьте все за раз:

```bash
# 1. Локальный бот
ps aux | grep PocSocSig_Enhanced.py

# 2. Переменные окружения (если локально)
cat .env | grep BOT_TOKEN

# 3. Webhook статус (если знаете токен)
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

---

## Контакты

Если проблема не решается:
1. Проверьте логи на Render
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что requirements.txt актуален

---

**Последнее обновление:** 2025-01-XX

